function slid(name,t1,t2,x0,y0) {


  g=1*windowHeight/name.height;

  if( t> (t2-5)){      }  
  


    if(t > t1 && t< t2)        { 
      image(name,+x0+10*cos(i/80),+y0+10*sin(i/80),name.width*g,name.height*g);
      image(name,x0+10*sin(i/80),y0+10*cos(i/80),name.width*g,name.height*g);
  
      /*
      image(name,0,0,windowWidth,windowHeight,10*cos(i/80),10*sin(i/80));
      image(name,0,0,windowWidth,windowHeight,10*sin(i/80),10*cos(i/80));
      */
    }



    
  }

  function image(name,t1,t2,x0,y0) {




    if( t> (t2-5)){      }  
    
  
  
      if(t > t1 && t< t2)        { 
        image(name,x0+10*cos(i/80),y0+10*sin(i/80));
        
    
        /*
        image(name,0,0,windowWidth,windowHeight,10*cos(i/80),10*sin(i/80));
        image(name,0,0,windowWidth,windowHeight,10*sin(i/80),10*cos(i/80));
        */
      }
  
  
  
      
    }







































































  

  //animation_1(img10,90,130,1,10,1);






// the image stay 10 second 
 //image(img8,0,0,windowWidth,windowHeight); // the image stay 10 second 

 // presentation de l'image 2 fois 
 //image(img9,0,0);
 //image(img9,windowWidth/2,0);


 //image(img10, windowWidth-img10.width-150,0,1000/1.5,1600/1.5);

 //image(img11,-0,0,img11.width*1.2,img11.height*1.2);  // ajustement Hight exact

 //image(img12,0,0,img12.width*1.2,img12.height*1.2); // ajustement Hight exact

 //image(img13,0,0,img13.width*1.2,img13.height*1.2); // ajustement Hight exact



  //image(img, 0, 0,);

 
  //image(img, 0, 0);
  // Displays the image at point (0, height/2) at half size
 // image(img, 2*i-5*sin(i/10),50+ i, img.width / (1), img.height / (1));
  ///////////////////////////////////////////////////////////////////////
 //image(img, windowWidth-2*i, 2*i, img.width / (1)-i/5, img.height / (1)-i/5);





////////////////////////////////////////////////////////////////////////

  ///////////////////////////////////////////////////
 // image(img, i+5*sin(i/10), 2*i, img.width +i/5, img.height +i/5);

 // image(img, windowWidth-i, i, img.width , img.height );
 
 // image(img, windowWidth-i, 3*i, img.width , img.height);
 
  